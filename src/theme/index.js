const colors = {
    dark: '#252525',
    transparentDark: 'rgba(37, 37, 37, 0.7)',
    primary: '#fff',
    secondary: '#6e7073',
    highlight: '#DF1E3A',
}

const themeApp = {
    backgroundHeader: colors.dark,
    backgroundBody: colors.dark,
    backgroundTabBar: colors.transparentDark,
}

const colorTab = {
    activeColor: colors.highlight,
    inactiveColor: colors.secondary
}

export const theme = {
    colors,
    themeApp,
    colorTab
}